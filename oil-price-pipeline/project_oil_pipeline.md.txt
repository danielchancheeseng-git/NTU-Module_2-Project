---
name: Oil Price Pipeline Project
description: Full status, stack, file locations, and GCP config for the oil price geopolitical pipeline
type: project
---

## Status: COMPLETE (all 5 steps done as of 2026-03-16)

## Stack
CSV → DuckDB → Meltano → BigQuery → dbt → Dagster → Power BI / Jupyter

## Project Locations
- Windows ingestion script: `C:/Users/rakhi/OneDrive/Documents/NTU/Module 2/Project Sample 2/oil-price-pipeline/ingestion/load_to_duckdb.py`
- WSL Meltano project: `~/meltano-ingestion/`
- WSL dbt project: `~/meltano-ingestion/oil_pipeline_dbt/`
- WSL Dagster project: `~/meltano-ingestion/oil_pipeline_dagster/`
- WSL EDA notebook: `~/meltano-ingestion/oil_eda.ipynb`
- WSL source CSVs: `~/projects/oil-price-pipeline/data/`
- WSL DuckDB: `~/projects/oil-price-pipeline/data/oil_pipeline.duckdb`

## GCP Config
- Project ID: `module-2-project-490315`
- BigQuery dataset: `oil_pipeline_raw`
- Location: US
- Auth: ADC (Application Default Credentials) — JSON key creation blocked by org policy
- ADC credentials at: `~/.config/gcloud/application_default_credentials.json`

## Conda env
`conda activate meltano-env` (Python 3.11 in WSL)

## Key fixes applied
- `setuptools==69.5.1` installed in target-bigquery venv to fix `pkg_resources` error
- meltano.yml config key is `project` not `project_id` for z3z1ma target-bigquery
- `denormalized: true` required in target-bigquery config for flat BigQuery columns
- Use `python3 - << 'PYEOF'` for writing files — bash heredoc gets corrupted on paste

## meltano.yml (final config)
- Extractor: `tap-csv` (meltanolabs), csv_files_definition: csv_files_definition.json
- Loader: `target-bigquery` (z3z1ma), project, dataset, location: US, method: storage_write_api, denormalized: true, overwrite: true

## BigQuery Tables
- `raw_oil_prices` — 4047 rows (raw)
- `raw_geo_events` — 35 rows (raw)
- `stg_oil_prices` — dbt view (cleaned/cast)
- `stg_geo_events` — dbt view (cleaned/cast)
- `mart_oil_geopolitics` — dbt view (joined oil + events)

## To restart services
- Pipeline: `cd ~/meltano-ingestion && meltano run tap-csv target-bigquery`
- dbt: `cd ~/meltano-ingestion/oil_pipeline_dbt && dbt run`
- Dagster UI: `cd ~/meltano-ingestion/oil_pipeline_dagster && dagster dev` → http://localhost:3000
- Jupyter: `cd ~/meltano-ingestion && jupyter lab --no-browser --port=8888` → http://localhost:8888
- Power BI: Get Data → Google BigQuery → project: module-2-project-490315 → oil_pipeline_raw